actor Insert () int(size = 8) input, int(size=13) dct_input ==> int(size = 13) output:
    @native function char_to_ascii(int b) --> int(size = 13) end
	action input:[a] ==> output:[char_to_ascii(a)]
	guard
		a > 0
	do
		println("Insert: The data is " + a);
	end
	
	action input:[a] ==> output:[a]
	guard
		a = 0
	do
	end
end